# Cursor Extensions

## List All Extensions

To list all installed extensions in Cursor:

```bash
cursor --list-extensions
```

## Save Extensions to File

```bash
cursor --list-extensions > cursor/extensions.txt
```

This command outputs all installed extension IDs in the format `publisher.extension-name`.

os:
  edit: "nvr -l {{filename}}"
  editAtLine: "nvr -l +{{line}} {{filename}}"
  editAtLineAndWait: "nvr -l --remote-wait +{{line}} {{filename}}"

promptToReturnFromSubprocess: false

git:
  paging:
    colorArg: always
    pager: delta --dark --paging=never

keybinding:
  commits:
    moveDownCommit: "J"
    moveUpCommit: "K"

gui:
  scrollHeight: 20
  nerdFontsVersion: "3"
  skipRewordInEditorWarning: true
